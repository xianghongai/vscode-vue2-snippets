{
	"Vue: useRoute()": {
		"prefix": "useRoute",
		"body": ["const route = useRoute();"],
		"description": "**Vue Router 4 only — `vue-router` 3.x has no composables.**\nOn Vue Router 3.x read the current route from `getCurrentInstance().proxy.$route`, or keep using `this.$route` in the Options API.\n"
	},
	"Vue: useRouter()": {
		"prefix": "useRouter",
		"body": ["const router = useRouter();"],
		"description": "**Vue Router 4 only — `vue-router` 3.x has no composables.**\nOn Vue Router 3.x reach the router through `getCurrentInstance().proxy.$router`, or keep using `this.$router` in the Options API.\n"
	},
	"Vue: useLink()": {
		"prefix": "useLink",
		"body": [
			"const { href, isActive, isExactActive, navigate, route } = useLink({",
			"  to: ${1:'/pathname'}",
			"});"
		],
		"description": "**Vue Router 4 only — `vue-router` 3.x has no composables.**\nOn Vue Router 3.x use `<router-link>` with its scoped-slot API (`v-slot=\"{ href, isActive, isExactActive, navigate, route }\"`), which gives the same values.\n格式和 `<router-link>` 的 `to` prop 一样，能被 `router.resolve` 解析。\n"
	}
}
